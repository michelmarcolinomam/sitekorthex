/**
 * As classes dos campos do painel do cliente. Ficam num módulo só porque são
 * usadas pelo formulário do lead e pelo painel operacional, que hoje vivem em
 * telas diferentes.
 */
export const CAMPO =
  "w-full rounded-md border border-border bg-background px-4 py-3 text-sm outline-none transition-colors placeholder:text-foreground/30 focus:border-primary";
export const ROTULO = "mb-2 block text-[10px] uppercase tracking-[0.2em] text-foreground/50";
export const BOTAO =
  "rounded-full bg-primary px-8 py-4 text-[11px] uppercase tracking-[0.2em] text-white transition-colors hover:bg-primary/90 disabled:opacity-40";
